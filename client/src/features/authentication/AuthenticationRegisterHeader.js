import FormHeader from 'features/forms/FormHeader';

const AuthenticationRegisterHeader = () => (
  <FormHeader title='Become a member' subtitle='Sign up to access your account' />
);

export default AuthenticationRegisterHeader;
