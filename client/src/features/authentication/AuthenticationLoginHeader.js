import FormHeader from 'features/forms/FormHeader';

const AuthenticationLoginHeader = () => (
  <FormHeader title='Welcome back!' subtitle='Sign in to access your account' />
);

export default AuthenticationLoginHeader;
